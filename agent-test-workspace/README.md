# Agent Test Workspace

这是给 coding agent 使用的独立测试项目。

## 当前任务

请检查 `calculator.py`，修复其中的问题，并补充必要的测试。

期望行为：

- `add(2, 3)` 返回 `5`
- `subtract(8, 3)` 返回 `5`
- `multiply(4, 3)` 返回 `12`
- `divide(8, 2)` 返回 `4`
- 除数为 `0` 时抛出 `ValueError`
- 命令行运行 `python calculator.py 8 / 2` 时输出 `4`

先阅读代码并说明问题，再进行修改。
